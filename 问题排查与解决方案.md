# ACE-RAG 问题排查与解决方案总览

> 本文档记录 ACE-RAG 系统在生产环境中发现的问题、根因分析和技术解决方案。
> 后续发现新问题时，在"问题索引表"追加一行，并在文档末尾追加详情章节。

---

## 问题索引表

| 编号 | 问题标题 | 严重程度 | 状态 | 发现日期 | 涉及题型 |
|------|----------|----------|------|----------|----------|
| ISSUE-001 | 多选题合并检索返回"证据不足" | 高 | 已修复 | 2026-08-04 | Q201、Q202、Q207、Q219、Q225 |
| ISSUE-002 | 复合查询理解能力不足（多选题语义稀释） | 高 | 已修复 | 2026-08-04 | 同上 |
| ISSUE-003 | 歧义警告时直接拒答，未利用强语境检索 | 中 | 已修复 | 2026-08-04 | 全局 |
| ISSUE-004 | 简写/不标准文档名导致检索0命中 | 高 | 已修复 | 2026-08-04 | 全局 |
| ISSUE-005 | Excel多选题0.1s快速空拒答，未走语义检索 | 高 | 已修复(待重启生效) | 2026-08-04 | 普惠型小微企业贷款等Excel多选题 |

---

## ISSUE-001: 多选题合并检索返回"证据不足"

### 1. 问题描述

**涉及题号**：Q201、Q207、Q219、Q225（同类故障 Q202）

**关联文档**：《附件 1：寿险合同负债评估的折现率曲线.pdf》

**现象**：
- 将完整多选题题干合并一次性检索时，系统返回"检索证据不足，无充分相关信息"
- 将题干中单条语句拆分单独检索时，系统可正常命中文档并返回标准答案
- 已核对知识库 JSONL 配置文件无错误

**业务人员疑问**：是否因为题干内存在某一条内容单独检索无法命中，就会导致整道合并式题目判定证据不足、判定内容未在知识库内？

### 2. 根因分析

**核心结论**：问题 **不是** "某一条内容不在知识库导致整体判定不足"，而是 **实体抽取器从多选项的书名号《》中误提取了 doc_name，作为元数据精确过滤条件后清空了全部检索结果**。

#### 根因链（逐层定位）

```
用户输入完整多选题题干
  │
  ▼
① EntityExtractor 用正则 r"《([^》]+)》" 匹配所有书名号内容
  │  题干中有两个《》：
  │    《寿险合同负债评估折现率曲线》→ doc_name #1
  │    《保险公司偿付能力监管规则第 19 号：保险集团》→ doc_name #2
  ▼
② RequestHandler._build_filters() 遍历实体
  │  filters["doc_name"] = value  ← 循环覆盖，只保留最后一个
  │  最终 filters = {"doc_name": "保险公司偿付能力监管规则第 19 号：保险集团"}
  ▼
③ RetrievalClient 将 filters 随检索请求发送给检索服务
  ▼
④ 检索服务 search_request() Phase 0 元数据过滤
  │  用 doc_name="保险公司偿付能力监管规则第 19 号：保险集团" 精确匹配
  │  知识库中文档名为 "460_中国银保监会关于实施_保险公司偿付能力监管规则（II）..."
  │  精确匹配失败 → 过滤后 0 条 chunk
  ▼
⑤ 返回空结果 → evidence_count=0 → sufficiency_score=0.0 < 0.60
  ▼
⑥ EvidenceBuilder 判定证据不足 → is_sufficient=False
  ▼
⑦ 系统走 REFUSING 分支，生成拒答回答
```

#### 为什么单句检索成功？

单句"寿险合同负债评估中计算现金流现值所采用的折现率曲线由基础利率曲线加综合溢价形成。准确吗"中 **没有书名号《》**，EntityExtractor 不提取 doc_name，filters 为空，检索服务走全库检索，正常命中。

#### 次要问题（不致命但需关注）

即使修复了 doc_name 过滤问题，完整多选题检索仍存在 **多主题语义稀释** 的次要问题：
- 完整题干包含 4 个不同主题选项（折现率曲线、保险集团偿付能力报告编报、人保集团等）
- BM25 关键词被多主题稀释，Dense 向量语义被平均化
- 检索结果 top_k 被多个主题的混合结果占据，与每个具体选项的相关性都偏低
- 这会导致证据质量下降，但不至于完全拒答（因为最相关的结果仍能排第一）

### 3. 验证过程

#### 验证 1：直接调用检索 API（绕过 Agent）

```
完整题干 → POST /api/v1/search (无 filters) → 命中 10 条（第1条正确）
单句A选项 → POST /api/v1/search (无 filters) → 命中 10 条（前5条全部精准）
```

结论：检索服务本身对完整题干能命中结果，问题出在 Agent 层。

#### 验证 2：调用 Agent API（完整流程）

```
完整题干 → POST /api/v1/query →
  intent:         definition
  is_refusal:     True
  evidence_count: 0          ← 关键！证据为空
  sufficiency:    0.0
  latency_ms:     21.9       ← 极快，检索未正常执行
```

#### 验证 3：Retrieval 容器日志

```
[检索路由] query="根据《寿险合同负债评估折现率曲线》..."  strategy=hybrid  top_k=20
[Phase 0] 元数据过滤 ...  filters={'doc_name': '保险公司偿付能力监管规则第 19 号：保险集团'}
[Phase 0] 过滤后无匹配 chunk，返回空结果  (0.016s)
```

结论：Agent 发送了检索请求，但带了错误的 doc_name 过滤条件，导致 Phase 0 元数据过滤清空结果。

#### 验证 4：意图分类

完整题干中 C 选项"其他符合保险集团**定义**的保险集团"中的"定义"一词触发了 `definition` 意图规则（`r"(?:什么是|什么叫|什么意思|是指|定义|含义|概念)"`），导致 intent 被误分类为 `definition`。

### 4. 技术解决方案

#### 方案 A：短期修复（推荐立即实施，改动最小）

**修改文件**：`agent_platform/gateway/request_handler/handler.py` 的 `_build_filters()` 方法

**修改点**：当存在多个 doc_name 实体时，不使用 doc_name 过滤（多选题场景中多个书名号不代表文档限定）

```python
# 修改前（当前代码）：
for entity in query_spec.entities:
    etype = entity.get("entity_type")
    value = entity.get("value")
    if etype == "doc_name" and value:
        filters["doc_name"] = value

# 修改后：
doc_names = [e.get("value") for e in query_spec.entities
             if e.get("entity_type") == "doc_name" and e.get("value")]
if len(doc_names) == 1:
    # 单个文档名：作为过滤条件（用户明确指定了文档来源）
    filters["doc_name"] = doc_names[0]
# 多个文档名：不设置过滤（多选题场景，避免误限定检索范围）
```

**预期效果**：完整多选题题干有两个书名号 → 不设 doc_name 过滤 → 全库检索 → 命中结果 → 证据充分 → 正常回答

**风险**：极低。仅影响多书名号查询的过滤逻辑，单书名号查询行为不变。

#### 方案 B：短期修复（补充，doc_name 模糊匹配）

**修改文件**：检索服务 `knowledge_platform/retrieval/retrieval_service/retrieval_api.py` 或 metadata 检索器

**修改点**：将 doc_name 过滤从精确匹配改为模糊匹配（LIKE / 子串匹配）

```python
# 当前：精确匹配，doc_name="保险公司偿付能力监管规则第19号" 匹配失败
# 修改后：子串匹配或 LIKE 查询
# WHERE doc_name LIKE '%保险公司偿付能力监管规则%'
```

**预期效果**：即使用户引用的文档名与知识库中的不完全一致，也能匹配到相关文档。

#### 方案 C：中期优化（查询分解）

**修改文件**：`agent_platform/query_understanding/` 新增查询分解器

**修改点**：识别多选题模式（含"下列哪项"、多个选项 A/B/C/D），自动拆分为子问题，每个选项独立检索、独立判断证据充分性

```
完整题干 → 查询分解 → [
  "寿险合同负债评估中计算现金流现值所采用的折现率曲线由基础利率曲线加综合溢价形成",
  "列入名单的保险集团应当按照...有关规定编报保险集团偿付能力报告",
  "其他符合保险集团定义的保险集团暂不编报偿付能力报告",
  "中国人民保险集团股份有限公司属于应当编制保险集团偿付能力报告的保险控股型集团"
]
→ 每个独立检索 → 独立验证 → 逐选项回答
```

**预期效果**：每个选项都能精准检索，系统可以逐选项给出"正确/错误"判断及依据。

#### 方案 D：长期优化（架构升级）

实现 README 中 Phase 4 设计的 Query Compiler + DAG 执行器，支持复杂查询的编译式执行计划和推测式检索。

### 5. 建议实施路径

```
立即 → 方案 A（修复 _build_filters，10分钟内完成）
补充 → 方案 B（doc_name 模糊匹配，提高容错性）
近期 → 方案 C（查询分解，根治多选题场景）
远期 → 方案 D（Query Compiler + DAG，全面提升复杂查询能力）
```

### 6. 涉及代码文件

| 文件 | 位置 | 说明 |
|------|------|------|
| `agent_platform/query_understanding/entity_extractor/extractor.py` | DOC_NAME_PATTERN 正则 | `r"《([^》]+)》"` 匹配所有书名号内容为 doc_name |
| `agent_platform/gateway/request_handler/handler.py` | `_build_filters()` 方法 | 循环覆盖 filters["doc_name"]，只保留最后一个 |
| `knowledge_platform/retrieval/retrieval_service/retrieval_api.py` | `search_request()` Phase 0 | 元数据精确过滤，无匹配则返回空 |
| `agent_platform/evidence/evidence_assembler/builder.py` | `_calculate_sufficiency()` | 证据为空时 sufficiency=0.0 |
| `agent_platform/query_understanding/__init__.py` | INTENT_RULES / _build_claims | "定义"关键词触发 definition 意图 |

### 7. 回答业务人员疑问

> "是否因为题干内存在某一条内容单独检索无法命中，就会导致整道合并式题目判定证据不足、判定内容未在知识库内？"

**不是。** 真正原因是：题干中 B 选项的 `《保险公司偿付能力监管规则第 19 号：保险集团》` 被实体抽取器提取为 `doc_name` 过滤条件，检索服务在元数据过滤阶段（Phase 0）用此条件精确匹配知识库文档名，匹配失败后清空了全部检索结果，导致证据为空、充分性评分为 0、系统判定证据不足并拒答。与"某一条内容是否在知识库内"无关，是过滤逻辑的缺陷。

---

## ISSUE-002: 复合查询理解能力不足（查询分解器）

### 问题
多选题含4个不同主题选项，合并检索时多主题语义稀释，BM25关键词被稀释、Dense向量被平均化。

### 解决方案：Query Decomposer 查询分解器

**新增模块**：`query_understanding/query_decomposer/decomposer.py`

**核心逻辑**：
- 识别多选题模式（"下列哪项" + A/B/C/D选项），自动拆分为独立子问题
- 每个子问题独立检索，避免多主题语义稀释
- 子问题检索时不带 doc_name 过滤（配合 ISSUE-001 修复）
- 合并所有子问题命中结果，按 chunk_id 去重
- 题干中的书名号《》上下文自动拼接到每个子问题前

**集成点**：`handler.py` 正常检索分支，`QuerySpecBuilder.build()` 中自动检测

**验证结果**：完整多选题题干 → 拆分4个子问题 → 各检索20条 → 合并去重38条 → 证据充分性1.0 → 正常回答

---

## ISSUE-003: 歧义警告时直接拒答，未利用强语境检索

### 问题
触发歧义警告时（如术语多义、范围缺失），系统直接返回澄清请求，不做检索。但问题中往往已有强语境信号（文档名、条款号、指标名），可以直接检索。

### 解决方案：ContextAnchorExtractor 语境锚点提取器

**新增模块**：`query_understanding/context_anchor/extractor.py`

**核心逻辑**：
- 从问题中提取"关联强语境"锚点：文档名(w=0.9) > 条款号(0.85) > 指标名(0.8) > 日期(0.6) > 术语(0.5)
- 当触发歧义警告时，先检查是否存在强语境锚点（weight≥0.7）
- 有强语境 → 用锚点构建增强查询，先做一轮检索
  - 证据充分 → 跳过澄清，直接生成回答
  - 证据不足 → 回退到澄清流程
- 无强语境 → 直接返回澄清请求（原有逻辑）

**集成点**：`handler.py` 歧义分支（分支1），在 `generate_clarification` 之前插入强语境检索

**关键方法**：
- `extract()` — 提取锚点
- `build_enhanced_query()` — 锚点增强查询
- `build_anchor_filters()` — 仅取单个最高权重 doc_name 作为过滤
- `has_strong_context()` — 判断是否存在强语境

---

## ISSUE-004: 简写/不标准文档名导致检索0命中

### 问题
用户用简写附件名+PDF后缀（无官方编号、完整发文标题）检索，doc_name 过滤匹配失败，Phase 0 返回0条，证据/充分性/置信度全为0。仅替换为库内标准全称即可正常命中。

### 根因
DB层 doc_name 已用 `LIKE %value%` 子串匹配，但用户简写名与存储名差异较大（缺字、缺前缀、带.pdf后缀、标点差异）时子串匹配仍失败，Phase 0 直接返回空结果。

### 解决方案：三层防御

**1. Agent层 — doc_name 归一化 + 别名解析**（`handler.py`）
- 新增 `_resolve_doc_name()` 方法，设置 doc_name 过滤前先归一化：去 .pdf/.docx 后缀、全角标点统一
- 新增 `DocNameResolver`（`doc_name_resolver.py`），从 DB 文档表自动构建别名注册表：去编号前缀、提取附件名、去发文机关前缀、关键词重合匹配

**2. 检索服务层 — Phase 0 自动回退**（`retrieval_api.py`）
- 当 doc_name 过滤返回0条时，自动去掉 doc_name 重试
- 若去掉 doc_name 后无其他过滤条件，回退全库检索
- 日志输出 `[Phase 0] doc_name 过滤无匹配，回退全库检索`

**3. 别名注册表**（`doc_name_resolver.py`）
- 从31篇文档名自动派生别名（如 `460_..._附件1：寿险合同负债评估的折现率曲线.pdf` → `附件1：寿险合同负债评估的折现率曲线`、`寿险合同负债评估的折现率曲线`）
- 支持精确匹配、归一化匹配、子串匹配、关键词重合匹配

### 验证结果
- 不匹配文档名 `不存在的文档名xxx` + doc_name过滤 → 回退全库 → 命中5条（之前0条）
- 简写文档名 `《寿险合同负债评估的折现率曲线.pdf》` 通过Agent查询 → evidence_count=11, sufficiency=1.0, confidence=0.95 → 正常回答

---

## ISSUE-005: Excel多选题0.1s快速空拒答，未走语义检索

### 1. 问题描述

**涉及题型**：Excel 附件类多选题（如普惠型小微企业贷款季度数据）

**复现问题**：
> 根据 Excel 附件《2023 年银行业金融机构普惠型小微企业贷款情况（季度）》（工作表：普惠型小微企业贷款），在 "年 - 季度" 口径下，以下哪一项数值最高？
> 选项 A: 城市商业银行  选项 B: 股份制商业银行  选项 C: 银行业金融机构合计  选项 D: 大型商业银行

**现象**：
- 系统在 **0.1s 级** 返回"检索证据不足"拒答
- latency 极短 → 强烈暗示**语义检索根本没执行**
- 即便把问题描述改成具体季度，仍然拒答

### 2. 根因分析

**核心结论**：不是"语义检索没召回"，而是 **检索请求在 Phase 0 元数据过滤阶段就被清空并提前 return，根本没进入 Phase 1 的 BM25/Dense 语义检索**；Agent 层也没有"空结果时重试"的兜底，直接判证据不足拒答。

#### 根因链

```
用户输入Excel多选题题干
  │  （含书名号《2023年...普惠型小微企业贷款情况（季度）》+ 工作表名"普惠型小微企业贷款"）
  ▼
① 实体抽取 → filters = {"doc_name": "2023年...普惠型小微企业贷款情况（季度）",
                          "sheet_name": "普惠型小微企业贷款", ...}  (多过滤条件叠加)
  ▼
② 检索服务 search_request() Phase 0 元数据过滤
  │  知识库实际文档名带编号前缀+后缀，如 "xxx_2023年银行业金融机构普惠型小微企业贷款情况（季度）.xlsx"
  │  doc_name 子串匹配失败 + sheet_name 等条件叠加 → 过滤后 0 条 chunk
  ▼
③ Phase 0 直接 `return []`（旧代码：if not allowed: return []）
  │  ★ 未进入 Phase 1 语义检索（BM25/Dense/RRF 全部跳过）★
  ▼
④ Agent 收到 hit_count=0 → 旧代码无兜底，直接走 EVIDENCE_ASSEMBLING → REFUSING
  ▼
⑤ evidence_count=0, sufficiency=0.0 → 0.1s 级快速空拒答
```

**为什么 0.1s 就拒答？** 因为 Phase 0 过滤是纯元数据 SQL 操作（毫秒级），过滤失败后直接返回空，**完全没触发向量检索**。低延迟正是"没走语义检索"的典型信号。

#### 与 ISSUE-004 的区别
- ISSUE-004：单条 doc_name 过滤失败 → 已实现"去掉 doc_name 重试"
- ISSUE-005：**多过滤条件叠加**（doc_name + sheet_name + applicable_scope 等）全部失败时，ISSUE-004 的"只去 doc_name"仍不够，且 Agent 层缺兜底 → 需要两层防御

### 3. 技术解决方案：两层兜底

#### 第一层：检索服务层 — Phase 0 渐进式回退

**修改文件**：`knowledge_platform/retrieval/retrieval_service/retrieval_api.py` 的 `search_request()` Phase 0

**修改点**：过滤无匹配时逐步去掉条件，直到命中或全库检索，**不再直接 `return []`**

```python
# 旧代码（直接返回空，语义检索被跳过）：
if not allowed:
    return []

# 新代码：渐进式回退
if not allowed:
    _fallback_to_full = False
    # 第1步：去掉 doc_name 重试
    if "doc_name" in req.filters:
        relaxed_filters = {k: v for k, v in req.filters.items() if k != "doc_name"}
        if relaxed_filters:
            allowed = { ... self.metadata.search(relaxed_filters, ...) ... }
        else:
            _fallback_to_full = True
    # 第2步：去掉 doc_name 后仍无匹配 → 去掉所有过滤条件，全库检索
    if not _fallback_to_full and not allowed:
        _fallback_to_full = True
    if _fallback_to_full:
        allowed = None  # 下游 allowed is None 即不过滤，进入 Phase 1 语义检索
```

**日志特征**：`[Phase 0] 所有过滤条件均无匹配，回退全库检索 (N chunks)`

#### 第二层：Agent 层 — 无过滤语义检索兜底

**修改文件**：`agent_platform/gateway/request_handler/handler.py` 主检索分支

**修改点**：检索成功但 `hit_count == 0` 时，用 `filters={}` 重试一次，强制走全库语义检索，避免 0.1s 级快速空拒答

```python
# 检索完成后、失败处理之前插入：
if retrieval_result.success and retrieval_result.hit_count == 0:
    logger.info(f"[步骤] 首次检索证据不足 (hits=0, filters={filters or '无'})，触发无过滤语义检索兜底")
    retrieval_result = self._retrieval_client.search_by_spec(
        query_text=search_query,
        route_decision=route_decision,
        filters={},  # 去掉所有过滤条件，强制全库语义检索
    )
    logger.info(f"[步骤] 无过滤语义检索兜底完成 → {retrieval_result.hit_count} hits")
```

**设计意图**：即使第一层 Phase 0 回退因故未生效（如检索服务版本未更新），Agent 层仍能独立兜底；两层互为冗余。

### 4. 涉及代码文件

| 文件 | 位置 | 改动 |
|------|------|------|
| `knowledge_platform/retrieval/retrieval_service/retrieval_api.py` | `search_request()` Phase 0 | 多过滤条件全失败时渐进式回退到全库检索，不再 `return []` |
| `agent_platform/gateway/request_handler/handler.py` | 主检索分支（检索完成后） | `hit_count==0` 时用 `filters={}` 重试，语义检索兜底 |
| `deploy/handler.py` | 同上（部署暂存副本） | 同步相同改动 |
| `deploy/retrieval_api.py` | 同上（部署暂存副本） | 同步相同改动 |

### 5. 验证方法

**日志验证**（重启服务后复现该问题，应看到）：
```
[步骤] 检索完成 → 0 hits, 0.x ms, filters={'doc_name': '...', 'sheet_name': '...'}
[步骤] 首次检索证据不足 (hits=0, filters={...})，触发无过滤语义检索兜底
[Phase 0] 所有过滤条件均无匹配，回退全库检索 (N chunks)   # 第一层生效时
[步骤] 无过滤语义检索兜底完成 → K hits, xx ms
[步骤] 证据组装 → 充分性=x.xx, sufficient=True
```
若 `K > 0` 且 `sufficient=True`，说明兜底生效，不再 0.1s 空拒答。

**单测验证**（注入 Mock 检索客户端：首次返回空、二次返回有结果）：
- 断言 `search_by_spec` 被调用 2 次
- 断言第 2 次 `filters == {}`
- 断言 `response.evidence_count > 0`

> 注：当前开发沙箱缺 pydantic 等依赖，无法本地跑端到端；代码已通过 `ast` 语法检查。**改动需在业务部署环境重启服务后生效。**

### 6. 部署说明（重要）

本次代码改动已写入实际运行文件路径（非仅 deploy 暂存）：
- `agent_platform/gateway/request_handler/handler.py`
- `knowledge_platform/retrieval/retrieval_service/retrieval_api.py`

**必须重启 Agent 与 Retrieval 两个服务才能生效**，否则线上仍跑旧代码、仍会 0.1s 空拒答。

真实服务入口（Makefile 中的 `services.agent_api.main` / `services.retrieval_api.main` 已过时，`services/` 目录为空，请勿使用）：
- Agent API：`uvicorn agent_platform.server:app --host 0.0.0.0 --port 8000`
- Retrieval API：`uvicorn knowledge_platform.retrieval.retrieval_service.server:app --host 0.0.0.0 --port 8001`

若使用 docker-compose 部署：`docker-compose restart agent retrieval`（按实际服务名）。
若使用进程管理：重启对应的 uvicorn/gunicorn 进程。

### 7. 关于该Excel问题的答案

将问题描述改为具体季度（如"2023 年第四季度，在'年-季度'口径下哪一项数值最高"）后，兜底机制可命中该 Excel 表格 chunk 并正常作答。

就选项本身而言：**C：银行业金融机构合计** 必然最高——它是城市商业银行、股份制商业银行、大型商业银行等所有子类的加总，逻辑上必然 ≥ 任一子项。精确数值需系统检索该 Excel 表格后给出。

---

<!--
后续新问题在此分割线下方追加，格式参照 ISSUE-001
-->
