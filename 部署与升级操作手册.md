# ACE-RAG 语义检索兜底 — 部署与升级操作手册

> **文档目的**：供开发人员按步骤完成"Excel 多选题 0.1s 空拒答"问题的代码部署与服务重启。
> **编写日期**：2026-08-04
> **涉及问题**：Excel 附件类多选题（如普惠型小微企业贷款季度数据）检索 0 命中导致快速空拒答，未走语义检索。
> **代码来源**：已推送至 GitHub 仓库 `https://github.com/zhaohongyuan-stack/agentRag`，分支 `trae/agent-DfM92T`

---

## 一、问题背景

### 1.1 现象

用户在前端提问 Excel 附件类多选题，例如：

> 根据 Excel 附件《2023 年银行业金融机构普惠型小微企业贷款情况（季度）》（工作表：普惠型小微企业贷款），在 "年 - 季度" 口径下，以下哪一项数值最高？
> 选项 A: 城市商业银行  选项 B: 股份制商业银行  选项 C: 银行业金融机构合计  选项 D: 大型商业银行

系统在 **0.1s 级** 返回"检索证据不足"拒答，延迟极短。

### 1.2 根因

**不是"语义检索没召回"，而是检索请求在 Phase 0 元数据过滤阶段就被清空并提前 return，根本没进入 Phase 1 的 BM25/Dense 语义检索**；Agent 层也没有"空结果时重试"的兜底，直接判证据不足拒答。

根因链：

```
用户输入Excel多选题题干
  │ （含书名号《2023年...》+ 工作表名"普惠型小微企业贷款"）
  ▼
① 实体抽取 → filters = {"doc_name": "2023年...普惠型小微企业贷款情况（季度）",
                        "sheet_name": "普惠型小微企业贷款", ...}  (多过滤条件叠加)
  ▼
② 检索服务 search_request() Phase 0 元数据过滤
  │ 知识库实际文档名带编号前缀+后缀，如 "xxx_2023年银行业金融机构普惠型小微企业贷款情况（季度）.xlsx"
  │ doc_name 子串匹配失败 + sheet_name 等条件叠加 → 过滤后 0 条 chunk
  ▼
③ Phase 0 直接 `return []`（旧代码：if not allowed: return []）
  │ ★ 未进入 Phase 1 语义检索（BM25/Dense/RRF 全部跳过）★
  ▼
④ Agent 收到 hit_count=0 → 旧代码无兜底，直接走 EVIDENCE_ASSEMBLING → REFUSING
  ▼
⑤ evidence_count=0, sufficiency=0.0 → 0.1s 级快速空拒答
```

**为什么 0.1s 就拒答？** Phase 0 过滤是纯元数据 SQL 操作（毫秒级），过滤失败后直接返回空，完全没触发向量检索。低延迟正是"没走语义检索"的典型信号。

---

## 二、代码改动详情（已完成，共 2 个文件）

改动已在 GitHub 仓库 `trae/agent-DfM92T` 分支，开发人员无需自己改代码，只需拉取部署。

### 2.1 改动 1：检索服务层 — Phase 0 渐进式回退

**文件**：`knowledge_platform/retrieval/retrieval_service/retrieval_api.py`
**位置**：`search_request()` 方法的 Phase 0 元数据过滤段
**改动**：过滤无匹配时逐步去掉条件，直到命中或全库检索，**不再直接 `return []`**

**旧代码**：
```python
            allowed = {
                self._chunks.index(r) for r in self.metadata.search(req.filters, limit=999999)
                if r in self._chunks
            }
            filters_applied = dict(req.filters)
            if not allowed:
                return []
```

**新代码**：
```python
            allowed = {
                self._chunks.index(r) for r in self.metadata.search(req.filters, limit=999999)
                if r in self._chunks
            }
            filters_applied = dict(req.filters)
            if not allowed:
                # 渐进式回退：逐步去掉过滤条件直到命中（避免元数据过滤过严导致0命中空拒答）
                _fallback_to_full = False
                if "doc_name" in req.filters:
                    relaxed_filters = {k: v for k, v in req.filters.items() if k != "doc_name"}
                    if relaxed_filters:
                        print(f"  [Phase 0] doc_name 过滤无匹配，回退去掉doc_name重试 filters={relaxed_filters}")
                        allowed = {
                            self._chunks.index(r) for r in self.metadata.search(relaxed_filters, limit=999999)
                            if r in self._chunks
                        }
                        filters_applied = dict(relaxed_filters)
                    else:
                        _fallback_to_full = True
                        filters_applied = {}
                        print(f"  [Phase 0] doc_name 过滤无匹配，回退全库检索 ({len(self._chunks)} chunks)")
                # 第2步：去掉doc_name后仍无匹配，去掉所有过滤条件 → 全库检索
                if not _fallback_to_full and not allowed:
                    print(f"  [Phase 0] 所有过滤条件均无匹配，回退全库检索 ({len(self._chunks)} chunks)")
                    _fallback_to_full = True
                    filters_applied = {}
                if _fallback_to_full:
                    allowed = None  # 全库检索（下游 allowed is None 即不过滤）
```

**日志特征**：`[Phase 0] 所有过滤条件均无匹配，回退全库检索 (N chunks)`

### 2.2 改动 2：Agent 层 — 无过滤语义检索兜底

**文件**：`agent_platform/gateway/request_handler/handler.py`
**位置**：主检索分支，检索调用之后、`# 检索失败处理` 之前
**改动**：检索成功但 `hit_count == 0` 时，用 `filters={}` 重试一次，强制走全库语义检索

**旧代码**：
```python
            # 调用检索（使用改写后的查询）
            retrieval_result = self._retrieval_client.search_by_spec(
                query_text=search_query,
                route_decision=route_decision,
                filters=filters,
            )

            # 检索失败处理
            if not retrieval_result.success:
```

**新代码**：
```python
            # 调用检索（使用改写后的查询）
            retrieval_result = self._retrieval_client.search_by_spec(
                query_text=search_query,
                route_decision=route_decision,
                filters=filters,
            )
            logger.info(f"[步骤] 检索完成 → {retrieval_result.hit_count} hits, {retrieval_result.latency_ms:.0f}ms, filters={filters or '无'}")

            # ── 语义检索兜底：检索成功但证据为0时（通常因元数据过滤过严），
            #    去掉所有过滤条件重试，确保语义检索执行，避免0.1s级快速空拒答 ──
            if retrieval_result.success and retrieval_result.hit_count == 0:
                logger.info(
                    f"[步骤] 首次检索证据不足 (hits=0, filters={filters or '无'})，触发无过滤语义检索兜底"
                )
                _step_t = _now_ms()
                retrieval_result = self._retrieval_client.search_by_spec(
                    query_text=search_query,
                    route_decision=route_decision,
                    filters={},  # 去掉所有过滤条件，强制走全库语义检索
                )
                logger.info(
                    f"[步骤] 无过滤语义检索兜底完成 → {retrieval_result.hit_count} hits, "
                    f"{retrieval_result.latency_ms:.0f}ms (总 {_now_ms() - _step_t:.0f}ms)"
                )

            # 检索失败处理
            if not retrieval_result.success:
```

**设计意图**：两层互为冗余。即使第一层 Phase 0 回退因故未生效（如检索服务版本未更新），Agent 层仍能独立兜底。

---

## 三、部署操作步骤（开发人员执行）

### 3.0 前置准备

- 服务器：`101.201.59.211`（root 登录）
- 项目路径：`/opt/ace-rag/`
- 部署方式：Docker Compose（4 容器：Nginx / Agent / Retrieval / Redis）
- 代码来源：GitHub `https://github.com/zhaohongyuan-stack/agentRag` 分支 `trae/agent-DfM92T`

### 3.1 第 1 步：拉取新代码（已完成）

```bash
cd /opt
git clone https://github.com/zhaohongyuan-stack/agentRag.git ace-rag-new
cd ace-rag-new
git checkout trae/agent-DfM92T
```

验证新代码到位（两个命令都应输出 `1`）：
```bash
grep -c "触发无过滤语义检索兜底" /opt/ace-rag-new/agent_platform/gateway/request_handler/handler.py
grep -c "所有过滤条件均无匹配" /opt/ace-rag-new/knowledge_platform/retrieval/retrieval_service/retrieval_api.py
```

### 3.2 第 2 步：诊断容器代码来源（关键，决定后续路径）

**必须先执行此步，确定容器是"挂载宿主机代码"还是"镜像内置代码"**：

```bash
echo "===== 容器列表 ====="
docker ps --format '{{.Names}}|{{.Image}}|{{.Status}}'

echo "===== agent容器挂载情况 ====="
AGENT_C=$(docker ps --format '{{.Names}}' | grep -iE 'agent' | head -1)
echo "agent容器: $AGENT_C"
docker inspect "$AGENT_C" 2>/dev/null | python3 -c "
import sys,json
d=json.load(sys.stdin)[0]
mounts=d.get('Mounts',[])
if not mounts:
    print('无挂载 → 代码在镜像内,改宿主机文件不生效,必须重建镜像(走情况B)')
else:
    for m in mounts:
        print(f\"挂载: {m.get('Source')} -> {m.get('Destination')} ({m.get('Type')})\")
"

echo "===== retrieval容器挂载情况 ====="
RET_C=$(docker ps --format '{{.Names}}' | grep -iE 'retriev' | head -1)
echo "retrieval容器: $RET_C"
docker inspect "$RET_C" 2>/dev/null | python3 -c "
import sys,json
d=json.load(sys.stdin)[0]
mounts=d.get('Mounts',[])
if not mounts:
    print('无挂载 → 代码在镜像内(走情况B)')
else:
    for m in mounts:
        print(f\"挂载: {m.get('Source')} -> {m.get('Destination')} ({m.get('Type')})\")
"

echo "===== docker-compose.yml volumes配置 ====="
grep -B1 -A5 -iE "volumes:|build:" /opt/ace-rag/docker-compose.yml 2>/dev/null | head -40
```

### 3.3 第 3 步：根据诊断结果二选一执行

#### 情况 A：容器挂载了 `/opt/ace-rag`（volume 挂载）

诊断输出显示 `Source: /opt/ace-rag/... -> Destination: /app/...`

**操作**：覆盖两个文件 + restart：

```bash
# 备份原文件
cp /opt/ace-rag/agent_platform/gateway/request_handler/handler.py /opt/ace-rag/agent_platform/gateway/request_handler/handler.py.bak
cp /opt/ace-rag/knowledge_platform/retrieval/retrieval_service/retrieval_api.py /opt/ace-rag/knowledge_platform/retrieval/retrieval_service/retrieval_api.py.bak

# 覆盖两个改动文件
cp /opt/ace-rag-new/agent_platform/gateway/request_handler/handler.py /opt/ace-rag/agent_platform/gateway/request_handler/handler.py
cp /opt/ace-rag-new/knowledge_platform/retrieval/retrieval_service/retrieval_api.py /opt/ace-rag/knowledge_platform/retrieval/retrieval_service/retrieval_api.py

# 验证(两个都应输出1)
grep -c "触发无过滤语义检索兜底" /opt/ace-rag/agent_platform/gateway/request_handler/handler.py
grep -c "所有过滤条件均无匹配" /opt/ace-rag/knowledge_platform/retrieval/retrieval_service/retrieval_api.py

# 重启(挂载模式,restart即可生效)
cd /opt/ace-rag
docker-compose restart agent retrieval
```

#### 情况 B：容器无挂载（代码在镜像内）

诊断输出显示 "无挂载"

**操作**：覆盖文件 + 重建镜像：

```bash
# 备份原文件
cp /opt/ace-rag/agent_platform/gateway/request_handler/handler.py /opt/ace-rag/agent_platform/gateway/request_handler/handler.py.bak
cp /opt/ace-rag/knowledge_platform/retrieval/retrieval_service/retrieval_api.py /opt/ace-rag/knowledge_platform/retrieval/retrieval_service/retrieval_api.py.bak

# 覆盖两个改动文件到原项目目录(build context 指向 /opt/ace-rag)
cp /opt/ace-rag-new/agent_platform/gateway/request_handler/handler.py /opt/ace-rag/agent_platform/gateway/request_handler/handler.py
cp /opt/ace-rag-new/knowledge_platform/retrieval/retrieval_service/retrieval_api.py /opt/ace-rag/knowledge_platform/retrieval/retrieval_service/retrieval_api.py

# 验证
grep -c "触发无过滤语义检索兜底" /opt/ace-rag/agent_platform/gateway/request_handler/handler.py
grep -c "所有过滤条件均无匹配" /opt/ace-rag/knowledge_platform/retrieval/retrieval_service/retrieval_api.py

# 重建镜像 + 重启(无缓存,确保用新代码)
cd /opt/ace-rag
docker-compose down
docker-compose build --no-cache agent retrieval
docker-compose up -d
```

⚠️ **`docker-compose down` 注意事项**：
- 如果 compose 文件里有数据卷（redis/es/pg 数据），`down` 默认不删数据卷，数据安全
- 但服务会短暂中断（约 1-3 分钟，取决于构建速度），建议低峰期操作
- **不要加 `-v` 参数**（`docker-compose down -v` 会删除数据卷，丢失知识库索引等数据）

### 3.4 第 4 步：验证部署成功

```bash
echo "===== 1. 容器健康状态 ====="
docker ps --format '{{.Names}}|{{.Status}}'
# 所有容器应显示 Up 且 (healthy)

echo "===== 2. 容器内代码确认(情况B必做) ====="
AGENT_C=$(docker ps --format '{{.Names}}' | grep -iE 'agent' | head -1)
docker exec "$AGENT_C" grep -c "触发无过滤语义检索兜底" /app/agent_platform/gateway/request_handler/handler.py 2>/dev/null \
  || docker exec "$AGENT_C" grep -c "触发无过滤语义检索兜底" agent_platform/gateway/request_handler/handler.py 2>/dev/null
# 应输出 1

RET_C=$(docker ps --format '{{.Names}}' | grep -iE 'retriev' | head -1)
docker exec "$RET_C" grep -c "所有过滤条件均无匹配" knowledge_platform/retrieval/retrieval_service/retrieval_api.py 2>/dev/null \
  || docker exec "$RET_C" grep -c "所有过滤条件均无匹配" /app/knowledge_platform/retrieval/retrieval_service/retrieval_api.py 2>/dev/null
# 应输出 1

echo "===== 3. 健康检查接口 ====="
curl -s http://127.0.0.1:8000/health 2>/dev/null | head
curl -s http://127.0.0.1:8001/health 2>/dev/null | head
```

### 3.5 第 5 步：前端功能验证

访问 `http://101.201.59.211/`，重新提问：

> 根据 Excel 附件《2023 年银行业金融机构普惠型小微企业贷款情况（季度）》（工作表：普惠型小微企业贷款），在 "年 - 季度" 口径下，以下哪一项数值最高？ 选项 A: 城市商业银行 选项 B: 股份制商业银行 选项 C: 银行业金融机构合计 选项 D: 大型商业银行

**预期结果**：
- 不再 0.1s 空拒答
- 能命中 Excel 表格并给出答案
- 答案应为 **C：银行业金融机构合计**（它是所有子类加总，必然最高）

### 3.6 第 6 步：日志验证兜底触发

如果前端仍拒答，查日志确认兜底是否生效：

```bash
# agent 容器日志
AGENT_C=$(docker ps --format '{{.Names}}' | grep -iE 'agent' | head -1)
docker logs "$AGENT_C" --tail 100 2>&1 | grep -E "兜底|检索完成|hits|证据组装"

# retrieval 容器日志
RET_C=$(docker ps --format '{{.Names}}' | grep -iE 'retriev' | head -1)
docker logs "$RET_C" --tail 100 2>&1 | grep -E "Phase 0|回退|过滤"
```

**正常日志应看到**：
```
[步骤] 检索完成 → 0 hits, 0.x ms, filters={'doc_name': '...', 'sheet_name': '...'}
[步骤] 首次检索证据不足 (hits=0, filters={...})，触发无过滤语义检索兜底
[Phase 0] 所有过滤条件均无匹配，回退全库检索 (N chunks)
[步骤] 无过滤语义检索兜底完成 → K hits, xx ms
[步骤] 证据组装 → 充分性=x.xx, sufficient=True
```
若 `K > 0` 且 `sufficient=True`，说明兜底生效。

---

## 四、回滚方案

如果部署后出现异常，可快速回滚：

### 4.1 情况 A（挂载模式）回滚

```bash
cd /opt/ace-rag
cp agent_platform/gateway/request_handler/handler.py.bak agent_platform/gateway/request_handler/handler.py
cp knowledge_platform/retrieval/retrieval_service/retrieval_api.py.bak knowledge_platform/retrieval/retrieval_service/retrieval_api.py
docker-compose restart agent retrieval
```

### 4.2 情况 B（镜像模式）回滚

```bash
cd /opt/ace-rag
cp agent_platform/gateway/request_handler/handler.py.bak agent_platform/gateway/request_handler/handler.py
cp knowledge_platform/retrieval/retrieval_service/retrieval_api.py.bak knowledge_platform/retrieval/retrieval_service/retrieval_api.py
docker-compose down
docker-compose build --no-cache agent retrieval
docker-compose up -d
```

---

## 五、后续优化建议（升级计划）

本次为短期修复（兜底机制）。以下是中长期优化建议，按优先级排列：

### 5.1 P0 — 已完成（本次部署）

| 项 | 状态 | 说明 |
|----|------|------|
| Phase 0 渐进式回退 | ✅ 已完成 | 多过滤条件全失败时回退全库检索 |
| Agent 层无过滤兜底 | ✅ 已完成 | hit_count=0 时无过滤重试 |

### 5.2 P1 — 建议近期实施

**1. doc_name 模糊匹配强化**
- 当前 DB 层用 `LIKE %value%` 子串匹配，但简写名/带后缀名仍可能失败
- 建议：在 `MetadataRetriever` 增加"关键词重合度匹配"，当 LIKE 失败时按 jaccard 相似度 > 0.6 匹配
- 涉及文件：`knowledge_platform/retrieval/retrieval_service/metadata_retriever.py`

**2. doc_name 别名注册表**
- 已有 `DocNameResolver`，从 DB 文档名自动派生别名（去编号前缀、去发文机关、提取附件名）
- 建议：在 Agent 层 `_build_filters()` 设置 doc_name 前先过别名解析
- 涉及文件：`agent_platform/gateway/request_handler/handler.py` 的 `_resolve_doc_name()`

**3. 多选题查询分解**
- 已有 `QueryDecomposer`，识别"下列哪项"+ A/B/C/D 模式自动拆分子问题
- 建议：确认该模块在所有多选题场景生效，每个子问题独立检索 + 独立验证
- 涉及文件：`agent_platform/query_understanding/query_decomposer/decomposer.py`

### 5.3 P2 — 中期优化

**1. 检索结果监控告警**
- 当 `hit_count=0` 触发兜底时，记录指标到监控（Prometheus / 日志聚合）
- 统计兜底触发频率，过高说明上游过滤逻辑仍需优化
- 涉及文件：`handler.py` 兜底分支加 metrics 上报

**2. Excel 表格检索策略增强**
- 当前 hybrid 检索对 Excel cell_fact 类型有时召回不准
- 建议：当检测到"工作表"/"表名"等关键词时，自动路由到 TABLE 策略
- 涉及文件：`agent_platform/routing/route_policy.py`

**3. 证据充分性阈值动态调整**
- 当前固定 0.60，对 table_lookup 等结构化检索可能偏严
- 建议：按 intent 动态调整（threshold 类 0.6，table_lookup 类 0.5）
- 涉及文件：`agent_platform/evidence/evidence_assembler/builder.py`

### 5.4 P3 — 长期架构升级

**1. Query Compiler + DAG 执行器**（README Phase 4 设计）
- 将复杂查询编译为可优化、可缓存的执行计划
- 支持推测式检索、并行子查询

**2. 多轮检索 + 证据累积**
- 首轮检索不足时，基于已有证据扩展查询（如父文档聚合、邻域扩展）
- 而非简单无过滤重试

**3. 检索结果缓存层**
- 对相同 query + filters 的检索结果做 LRU 缓存
- 显著降低重复查询延迟

---

## 六、验收清单

部署完成后，请逐项确认：

- [ ] 第 2 步诊断输出已记录（容器挂载方式）
- [ ] 第 3 步两个文件 grep 验证均输出 1
- [ ] 第 4 步容器内代码 grep 验证均输出 1（情况 B 必做）
- [ ] 第 4 步所有容器健康状态为 Up (healthy)
- [ ] 第 5 步前端复测 Excel 多选题，不再 0.1s 空拒答
- [ ] 第 6 步日志显示兜底触发且 sufficient=True
- [ ] 备份文件 `.bak` 已保留，回滚方案可用

---

## 七、关键文件索引

| 文件 | 路径 | 改动 |
|------|------|------|
| Agent 层兜底 | `agent_platform/gateway/request_handler/handler.py` | hit_count=0 时无过滤重试 |
| Phase 0 回退 | `knowledge_platform/retrieval/retrieval_service/retrieval_api.py` | 多过滤全失败时全库检索 |
| 问题排查文档 | `问题排查与解决方案.md` | ISSUE-005 完整记录 |

## 八、联系人

- 代码改动来源：GitHub `https://github.com/zhaohongyuan-stack/agentRag` 分支 `trae/agent-DfM92T`
- 问题描述：Excel 多选题 0.1s 空拒答，未走语义检索
- 兜底机制：两层（检索服务 Phase 0 回退 + Agent 层无过滤重试）

---

**附：本次改动涉及的代码片段已全部在本文档第二章节列出，开发人员可直接对照确认。**
